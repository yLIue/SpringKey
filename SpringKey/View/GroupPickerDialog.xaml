<Window x:Class="SpringKey.View.GroupPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="选择分组" Height="320" Width="280"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        Background="{StaticResource ThemeColor}"
        ResizeMode="NoResize">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="选择目标分组"
                   Foreground="#CCCCCC" FontSize="13" Margin="0,0,0,15" />

        <ListBox Grid.Row="1" x:Name="GroupListBox"
                 Background="Transparent"
                 BorderBrush="{StaticResource BorderColor}"
                 BorderThickness="1"
                 Foreground="#D4D4D4" FontSize="13"
                 SelectionMode="Single" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="取消" Style="{StaticResource StyleButton}"
                    Foreground="#CCCCCC"
                    Margin="0,0,10,0" Width="60" Height="32"
                    Click="CancelButton_Click" />
            <Button Content="确定" Style="{StaticResource StyleSaveButton}"
                    Foreground="White"
                    Width="60" Height="32" Cursor="Hand"
                    Click="ConfirmButton_Click" />
        </StackPanel>
    </Grid>
</Window>
